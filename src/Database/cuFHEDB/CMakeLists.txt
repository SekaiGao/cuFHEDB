set(cuFHEDB_SOURCE_FILES ${cuFHEDB_SOURCE_FILES})

install(
    FILES
        ${CMAKE_CURRENT_LIST_DIR}/comparable_gpu.cuh
    DESTINATION
    ${cuHEDB_INCLUDES_INSTALL_DIR}/Database/cuFHEDB
)

set(cuFHEDB_SOURCE_FILES ${cuFHEDB_SOURCE_FILES} PARENT_SCOPE)
